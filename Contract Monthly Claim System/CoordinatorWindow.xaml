<Window x:Class="Contract_Monthly_Claim_System.CoordinatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Coordinator - CMCS"
        Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#FFF9F9F9">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Background="Crimson" Grid.Row="0" MouseDown="TopBar_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Programme Coordinator - Verify Claims"
                       Foreground="White" FontSize="14" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="10,0,0,0"/>

            <Button Grid.Column="1" Content="—" Width="35" Height="25"
                    Foreground="White" FontSize="14"
                    Click="Minimize_Click"/>
            <Button Grid.Column="2" Content="✕" Width="35" Height="25"
                    Foreground="White" FontSize="14"
                    Click="Close_Click"/>
        </Grid>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="40,40,40,0">
            <TextBlock Text="Welcome Coordinator, verify and accept claims submitted by lecturers."
                       FontSize="18" FontWeight="Bold" Foreground="#333"
                       TextAlignment="Center" Margin="0,0,0,20"/>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <Button Content="View Pending Claims"
                        Width="200" Height="50"
                        Background="Crimson" Foreground="White" Margin="0,0,0,15"/>
                <Button Content="Accept Claim"
                        Width="200" Height="50"
                        Background="Crimson" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>

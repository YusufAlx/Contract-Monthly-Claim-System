<Window x:Class="Contract_Monthly_Claim_System.TrackClaimsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Track Claim" Height="260" Width="420" Background="{StaticResource AppBackground}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Track Claim" Style="{StaticResource HeaderLabel}" Grid.Row="0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,8">
            <Label Content="Tracking ID:" VerticalAlignment="Center" Width="100"/>
            <TextBox x:Name="txtTrackingId" Width="220" Style="{StaticResource CrimsonTextBox}"/>
            <Button Content="Check" Click="Check_Click" Style="{StaticResource CrimsonButton}" Margin="6,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,12">
            <TextBlock x:Name="txtResult" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
